# R78 Capability Inventory Migration Archive

This archive records the R78 staged migration and R79 root-pointer cleanup for legacy root `capabilities/`, `runtime/`, `models/`, and `plugins/` directories.

It contains reference maps and migration decisions for root compatibility cleanup. It is historical evidence, not an active standard source and not a project-local binding template.

Active sources after R78:

- `standards/capability-inventory/`
- `workspace/organization/capability-inventory/`
- `examples/capability-inventory/`

R79 archived the root pointer README files under `root-pointers/`. Future development should not restore `capabilities/`, `runtime/`, `models/`, or `plugins/` as root facts sources.
