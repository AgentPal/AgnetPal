# Export Policy

Export is never implicit.

PalSmith must ask which export mode to use:

- Clean Pack Export
- With Memory Export
- Template Export
- Team Export
- Backup Export

Clean Pack Export is the default recommendation. With Memory Export requires strong confirmation and memory-scope selection.

Public release exports must exclude private memory, state, reports, credentials, tokens, absolute paths, and private project data.
