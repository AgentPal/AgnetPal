# Agent-use Decision Card

task_summary:

user_goal:

missing_information:

risk_level: low | medium | high | critical | blocked

privacy_or_external_write_risk:

owner_pal:

verifier_pal:

candidate_collaborators:

agent_host_recommendation:

host_capability_source:

codex_mode: normal_chat | plan_mode | goal_mode | owner_verifier | parallel_review | sequential_chain | external_agent_handoff | fallback

codex_mode_reason:

model_class_recommendation:

reasoning_strength: low | medium | high | xhigh | max | unknown

skill_plugin_candidates:

skill_plugin_invocation_plan:

skill_plugin_non_use_reason:

subthread_subagent_decision: single_thread | owner_verifier | parallel_review | subthread_candidate | subagent_candidate | not_recommended | unknown

subthread_subagent_reason:

context_packet_required: yes | no

task_package_required: yes | no

authorization_required:

next_action:

confidence: low | medium | high

residual_risks:

