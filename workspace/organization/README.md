# Organization

The organization directory is the home for central AgentPal roster and governance records.

The contacts source of truth is `workspace/organization/contacts/pals.json` plus the human-readable `workspace/organization/contacts/PAL_CONTACTS.md`.

Do not copy this directory into external user projects by default.
