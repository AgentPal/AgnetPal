# Release Audit Templates

This directory can hold future public-safe audit templates.

Current status: no reusable templates are required for v0.1.0-rc.1. Use the completed files under `release/v0.1.0-rc.1/` as the current example format.
