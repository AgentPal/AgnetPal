# AI Judgement Routing

AgentPal routing is case-by-case AI judgement.

The current AI considers the user goal, deliverable, project context, risk, Pal assets, and host runtime capability. Capability notes help judgement, but they are not fixed routes.

Explicit `/pal Name` and `@Name` calls use central contact aliases. Ordinary semantic routing must not be implemented as keyword matching.
