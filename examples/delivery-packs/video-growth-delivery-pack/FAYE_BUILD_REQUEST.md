# Faye Build Request

## Faye Build Request summary

Faye requests a public-safe Pal team skeleton plan for a short-video growth delivery team. This handoff is a no-code request for PalSmith judgement; it does not authorize roster mutation, official Pal edits, connector setup, or external project writes.

## Pal Team Blueprint

See `PAL_TEAM.md` for the candidate Pal team and its non-routing boundary.

## Requested Pal team

Faye requests that PalSmith create or propose a public-safe Pal team skeleton for:

- Research Pal
- Scriptwriter Pal
- Brand Guardian Pal
- Material Manager Pal
- Video QA Pal
- Publishing Pal
- Growth Review Pal

This request does not modify central roster by itself.

## PalSmith Build Scope

PalSmith may prepare a Pal creation package, target file checklist, missing information list, and public-safe placeholder skeleton plan for the requested Pal team. PalSmith must stop before any central roster update, official Pal update, or external project write unless a separate user-approved task explicitly authorizes that action.

## Required Pal assets

Each candidate Pal skeleton should include:

- root identity and boundary files;
- public-safe `pal.json` proposal if a later confirmed task allows it;
- `skills/index.md`;
- workflow index;
- runbook index;
- memory boundary;
- report boundary;
- eval or acceptance checklist;
- examples marked as public-safe placeholders.

## Pal Skill needs

- research brief creation;
- short-video script drafting;
- brand claim review;
- material inventory handling;
- video QA review;
- publishing handoff preparation;
- growth review and learning candidate extraction.

These are Pal Skill needs, not Runtime Agent Skills.

## Candidate Agent Skill refs

No Agent Skill, connector, browser automation, platform API, or external MCP capability is required for this example. Future runtime helpers may be listed only as evidence-backed candidates in a separate task.

## Workflow needs

- product intro video flow;
- founder talking video flow;
- customer case video flow;
- live clip flow;
- publishing review flow.

## Runbook needs

- missing source material runbook;
- claim-risk escalation runbook;
- private customer material refusal runbook;
- publishing blocker runbook;
- post-publication evidence review runbook.

## Missing information

- `missing`: real brand voice examples;
- `missing`: approved customer case permission;
- `missing`: platform account access policy;
- `unknown`: current publishing cadence;
- `not-run`: platform performance data review;
- `requires-human-review`: all content claims and publication decisions.

## Memory / report boundary

Reports are not memory until stable lessons are extracted.

Do not write full task reports, raw performance notes, private customer materials, or platform analytics into reusable Pal memory.

## Human review

Human review is required before:

- publishing content;
- using customer case material;
- making product performance claims;
- changing brand rules;
- promoting learning candidates into reusable assets.

## Blocked items

- real customer materials: blocked;
- real account credentials: blocked;
- platform API access: blocked;
- central roster modification: blocked;
- official Pal creation: blocked until a separate confirmed task exists;
- external project write: blocked by default.

## No credential / no connector / no external project write

This request must not include credentials, tokens, cookies, API keys, connector settings, API clients, platform automation, or writes into external project `.agentpal/`.

## Expected PalSmith output

PalSmith may produce:

- Pal team skeleton plan;
- allowed write paths for a later confirmed creation task;
- missing information list;
- public-safety review;
- not-run / blocked evidence list.

PalSmith must not claim files were created unless the current Runtime provides evidence.
