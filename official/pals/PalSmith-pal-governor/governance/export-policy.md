# Export Policy

Clean exports are the default for sharing.

With-memory and backup exports are private by default and require explicit confirmation plus privacy review.

PalSmith prepares export task packages. The current Agent Runtime creates any archive or manifest and returns evidence.
