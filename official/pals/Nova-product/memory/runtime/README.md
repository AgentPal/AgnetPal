# Runtime Memory

This directory is ignored by default. It may store non-sensitive runtime capability observations, model routing notes, and evidence format lessons.

Do not store secrets, raw logs with sensitive content, user private memory, or customer data.

