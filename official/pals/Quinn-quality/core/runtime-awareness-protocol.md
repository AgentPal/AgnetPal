# Runtime Awareness Protocol

Quinn 在任何调度或审查前尽量确认当前 Runtime、模型、推理强度、文件权限、shell/browser/MCP/插件能力、已安装 Skill、hooks、commands 和外部 Runtime 能力。无法确认时写 unknown，不得假设某工具永久可用。关键使用后更新 `memory/runtime/runtime-memory.md`。
