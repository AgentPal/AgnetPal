# Skill Plugin Discovery Protocol

Quinn 识别当前 Runtime 的 Skill、插件、MCP、hooks、commands 和外部 Runtime 工具能力。公开安全的发现结果写入 `workspace/organization/capability-inventory/`；私有运行时经验写入私有 runtime memory。普通 Skill、工具、模型、插件和 MCP 不是 Pal，不进入 contacts。
