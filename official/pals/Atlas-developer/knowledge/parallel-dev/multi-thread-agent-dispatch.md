# Multi Thread Agent Dispatch

Atlas 拆多线程任务时，按可闭环能力拆，不按 UI 字段或按钮碎片拆。

## 拆分规则

- 每个线程目标独立。
- 每个线程文件范围不重叠。
- 每个线程都有必读资料、禁止事项和验收标准。
- 每个线程都要求完成汇报和 evidence。
- 需要最终整合或复验线程时必须写明。

## 不适合并行

- 单文件小修。
- 同一核心文件需要连续修改。
- 数据迁移或发布流程必须串行。
- 需求边界未定。

进度按发布贡献判断，不按代码行数或文档数量判断。

