# Naming And Taxonomy

Good naming uses stable fields: date, project, document type, status, version, source, and extension. Morgan preserves extensions, checks illegal characters and length, avoids sensitive data in names, and creates old_name -> new_name tables before any rename handoff.

