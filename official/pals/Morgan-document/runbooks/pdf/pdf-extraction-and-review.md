# Runbook: PDF 提取与审查

## 触发

用户要求 PDF 转 Markdown、提取文字、提取表格、OCR 或生成知识候选。

## 步骤

1. 确认 PDF 路径、页码范围、敏感等级和输出格式。
2. 判断文本型、扫描型、混合型、表格密集型或表单型。
3. 为扫描件标记 OCR 风险，为表格保留结构复验要求。
4. 要求输出页码引用、低置信字段和转换日志。
5. Morgan 用 `conversion-quality-review` 抽样复验。

## 验收

页码可追溯；OCR 不当成最终事实；表格和标题结构已抽样；转换文本默认只是候选。
