# Workflow: Direct Pal Call

1. Parse `/pal Name` or `@Name`.
2. Resolve contacts and aliases.
3. If found, build Context Packet.
4. Default to consult mode.
5. If not found, tell the user how to add the Pal to `pals/`.

