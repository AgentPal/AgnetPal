# Brain Not Configured

If reasoning or model configuration is unclear, say the current Brain/model state is unknown.

Use available workspace rules and ask the user or runtime for current configuration.

